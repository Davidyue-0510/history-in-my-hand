name: 程序优化评估
about: 架构、性能、可维护性审计与补丁（tools/*.py 与 demo/county.js）
title: "[程序] "
labels: performance, needs-triage
assignees: ""
---

## 评估对象（文件 / 模块）
（如 `demo/county.js` 投影引擎 / `tools/build.py` 编译链）

## 现象 / 数据
（加载时长、单文件体积、卡顿、重复计算等可量化现象）

## 你的判断与建议
（最该先优化的 1–2 处，附理由；可接受「只给报告、不动代码」）

## 基准数据（若有）
（DevTools / `performance.now()` 测得的解析 + 首屏绘制时长；`demo/data.js` 体积）
