# 小菜狗的文明图景 · 协作与招募手册

> 本文件是对外招募与合作的总入口。项目代码在 `history-in-my-hand` 仓库，已上线 GitHub Pages。
> 仓库主（BDFL）：Davidyue-0510。本手册与 `README.md` / `CONTRIBUTING.md` 互为补充——`README` 讲"是什么"，`CONTRIBUTING` 讲"怎么改"，本手册讲"我们已经有什么、接下来要做什么、我们需要你"。

---

## 0. 一句话定位

**「小菜狗的文明图景」是一个"主张驱动"的历史可视化项目**：把明末清初辽东走廊的史实，拆成一条条可溯源的**断言（assertion）**，投影到**共享的真实地形**上。

核心主张只有一句，但它是整个工程的北极星：

> **立场靠来源派生**——系统绝不替读者下裁判，只把每条史实的"谁在说、离事件多远、敌方还是己方"投影出来，让读者自己看见冲突与共振。

这句话有两个工程前提（决定了我们为什么这么设计）：
1. **分类规则是单一真值**：立场/政权映射只写在 `data/vocab.json`，任何消费者都读它，不硬编码。
2. **数据是规整的**：脏数据会让上层静默给出错误答案，所以每一次改动都要过四道闸门。

---

## 1. 我们已经做完了什么

### 1.1 数据规模（截至 v0.6.1，已上线）
| 维度 | 数量 |
|---|---|
| 切片（可被独立查看的县域/事件单元） | **14** |
| 区域（建州/辽北/辽东/辽南/辽西） | **5** |
| 断言总数 | **208** |
| ├ 史料层 record | 129 |
| ├ 考据层 scholarship | 40 |
| ├ 推论层 inference | 10 |
| └ 缺口层 gap（= 研究线索） | 29 |
| 史源 | **17** |
| 地点 | **73** |
| 人物 | **62** |
| 冲突组（自动识别的史料矛盾） | **8** |
| 走廊（叙事主线） | **4** |

### 1.2 架构（已经跑通、可扩展）
- **声明式切片注册**：`data/scenes.json` 是唯一真值。加一个新县 = 写 `data/<县>/` 六件套文件（`sources/places/persons/events/edges/assertions`）+ 在 `scenes.json` 加一条注册。**前端零改动**，枢纽地图自动渲染。
- **通用县页**：`demo/county.html?scene=<key>`，14 个切片共用一套引擎。
- **四闸门工具链**（质量守门员，缺一不可）：
  - `tools/lint.py` —— 数据完整性（重复 id / 未登记词表 / 空引文 / 空值 / 命名空间挂钩）
  - `tools/test_assertions.py` —— 不变量回归（如：每个 `events.json` 的 subject 必须被 ≥1 条断言引用）
  - `tools/leads.py` —— 把"缺口断言"聚合成可认领的研究线索 `data/leads.json`
  - `tools/build.py` —— 编译进 `demo/data.js`
  - `tools/gates.py --strict` —— 一键跑以上四步，CI 用它
- **地形网格 + 诚实边界**：ASTER GDEM v3，覆盖 122–126.8°E / 40–43.3°N。网格外的县（如辽西/辽南 5 县）**绝不伪造高程**，地图留白并显式标"地形网格外"横幅。
- **CI 已闭环**：`.github/workflows/ci.yml`，PR/push 自动跑 `gates.py --strict`，非技术贡献者开 PR 即见红绿，无需本地装 Python。

### 1.3 协作基础设施（已就位，可直接对外）
- **真许可**：根 `LICENSE` 为全文 AGPL-3.0（代码）/ `LICENSE-data` 为 CC BY 4.0（数据）。
- **贡献契约**：`CONTRIBUTING.md` 含 DCO、设计契约（不可破的 5 条）、治理与冲突、署名、直引 `quotes` 约定。
- **门户招募入口**：`demo/portal.html` 内置三类角色入口 + **29 条可认领研究线索**，访客一眼能看到"哪里缺人、缺什么"。
- **已上线**：GitHub Pages 门户 `https://davidyue-0510.github.io/history-in-my-hand/demo/portal.html`，CI 首跑 `conclusion: success`。

---

## 2. Demo 当前状态与后续工作

### 2.1 现在能跑什么
- `portal.html`：枢纽地图（14 切片动态并集边界）+ 合作者入口 + 29 条线索清单 + 版本/统计。
- `county.html?scene=<key>`：单县详情——地形图、史料/考据/推论/缺口分层、来源立场着色、冲突标注、离线降级横幅。
- `hub.js` / `county.js`：动态投影，网格外县也能上图。
- `kaiyuan.css`：当前样式基线（辽东配色，但非系统化设计系统）。

### 2.2 Demo 后续工作（给美工/前端的清单）
- [ ] **设计系统**：把零散 CSS 变量化成一套可维护的配色/字号/间距 token（立场色板要能一眼区分 明方/清方/朝鲜/学界/敌方官修）。
- [ ] **断言详情卡**：点断言弹出结构化卡片（原文摘录、来源立场、置信度、冲突指向），目前只有基础渲染。
- [ ] **引文悬浮 / 抽屉**：`quote` 字段可在图上悬浮预览，不必跳页。
- [ ] **地图图层切换**：地形/政区/走廊/来源立场 多图层开关。
- [ ] **时间轴交互**：按 `era` 字段做可拖拽时间轴，滑动看走廊演变。
- [ ] **移动端适配**：当前桌面优先，门户在手机上可读性差。
- [ ] **叙事模式**：从"看地图"到"跟我走一遍宁远之战"的引导式叙事。
- [ ] **性能**：30+ 切片时的渲染与加载优化（见 §5-C 何时招程序）。

---

## 3. 之后整体的路线图（未来工作）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| 内容南推 | 补塔山/杏山/宁远卫/山海关等松锦-入关战场节点；把辽西走廊从"广宁—宁远"延伸到"山海关" | 数字人文 |
| 直引数字化 | 把断言的 `quote`（现有 paraphrase_unverified）逐步升级为 `quotes:[{text, source_id, ref}]` 逐字摘录；lint W11 已守着格式 | 数字人文 |
| 模型增强 | 共振度报告、冲突组可视化、来源依赖度压力测试（"抽掉某源系统会不会崩"） | 算法/程序 |
| 工具民主化 | 做一个**可视化编辑器**，让 DH 不写 JSON 也能加断言；批量导入古籍条目 | 程序 |
| 社区化 | issue 模板、`good-first-issue` / `help-wanted` 标签、月度同步 | 仓库主 |

> 设计原则不变：**缺口是一等公民**（别把 gap 偷改成断言而不补来源）；**网格外不伪造高程**；**立场只投影不裁判**。

---

## 4. 预期达到的效果

- **短期（招到人后 ~3 个月）**：29 条缺口大部分被认领填写；辽西走廊叙事完整；门户视觉达到"可对外演示"水准；直引字段在重点切片试点。
- **中期（~1 年）**：切片 14 → 30+；形成"明末清初辽东走廊"完整主张图谱；可视化编辑器上线，贡献门槛降到"会查史料就会填"。
- **长期**：成为数字人文领域一个**可审计、可辩论、立场透明**的示范案例——读者不仅能看历史，还能看"历史是怎么被不同来源讲出来的"。方法论（主张驱动 + 来源派生立场）可复用到其他时段/地域。

---

## 5. 我们需要招什么样的人

按"不对称招募"原则排序：**先数字人文，次美工，程序优化暂按住**。

### A. 数字人文 / 史料考证（★ 最优先，内容瓶颈）
- **需要技能**：明清史 / 东北边疆史功底；古籍与官修史书检索能力；史料批判意识（能判断"谁在说、离事件多远、为何这么说"）；不排斥写结构化 JSON（不需要会编程，照模板填即可）。
- **具体工作**：
  1. 从门户认领 29 条研究线索中的若干条，补成完整断言；
  2. 用 `seed_liaoxi.py` 思路加新切片（或照 `data/fushun/` 六件套抄模板）；
  3. 把重点断言的 `quote` 升级为逐字 `quotes:[{text, source_id, ref}]`；
  4. 写 scholarship 层（考据层）断言，标注学界分歧；
  5. 反向校验"立场靠来源派生"模型——它成立吗？哪里会塌？
- **上手路径**：Fork → 在门户挑一条 `good-first-issue` → 改 `data/<县>/assertions.jsonl` → 提 PR → 看 CI 红绿 → 我来审并合并。
- **为什么先招**：29 条缺口在等、且 DH 人能反过来检验整个内核是否成立。

### B. 界面 / 视觉设计（美工，低风险、隔离）
- **需要技能**：信息设计 / 数据可视化审美；CSS + SVG 熟练；地图配色与图例设计敏感度；能画交互原型。
- **具体工作**：见 §2.2 清单——设计系统 token 化、断言详情卡、来源立场色板、移动端、叙事模式视觉。
- **上手路径**：只动 `demo/kaiyuan.css` 与 SVG，**不碰数据契约**（见 §7 红线）。改动隔离在表现层，不会搞坏数据。
- **为什么次招**：工作边界清楚、风险低、能立刻提升"可展示度"，适合作为第二批。

### C. 程序 / 架构优化评估（暂按住，条件成熟再招）
- **需要技能**：前端工程（原生 JS / 轻框架）、可视化性能、数据管道。
- **触发条件**：切片到 30+ 出现性能投诉，或可视化编辑器要上马时。
- **具体工作**：性能剖析、编辑器原型、底图切换、来源依赖度压力测试的工程化。
- **为什么暂按住**：现在 14 切片没什么可优化的；早拉"重构党"会和 DH 抢方向、制造 churn。

---

## 6. 怎么加入

1. 读三份文件：`README.md`（是什么）→ `CONTRIBUTING.md`（怎么改）→ 本手册（招什么人）。
2. 在门户 `demo/portal.html` 看 29 条线索，挑你感兴趣的认领（开 issue 说一声即可）。
3. Fork → 改 `data/` 下对应 JSON → 提 PR。CI 会自动跑四闸门，红绿即时反馈。
4. 提交即视为同意以 AGPL-3.0 / CC BY 4.0 授权（DCO）；请在 commit 加 `Signed-off-by`。
5. 署名：在断言加 `contributor` 字段，并登记到 `CONTRIBUTORS.md` 荣誉榜。
6. 联系仓库主：[在此填写你的联系方式 / GitHub Discussions / 微信群 / 邮箱]。

---

## 7. 协作红线（设计契约 5 条，不可破）

1. **分类规则单一真值**：立场/政权映射只能改 `data/vocab.json`，别去 `county.js` / `resonance.py` 硬编码。
2. **网格外不伪造高程**：`OFF_GRID` 横幅必须保留，地形缺失就留白，绝不编数字。
3. **缺口是一等公民**：别把 gap 偷改成断言而不补来源；别删 `lead` 块。
4. **空字段是 bug**：空引文 / 空值在 UI 上渲染为空白、肉眼看不到，但会污染下游计算（lint E07/E10 守着）。
5. **命名空间挂钩**：`events.json` 的 `subject` 必须被 ≥1 条断言引用（lint I9），否则时间轴切不到史料。

**治理**：仓库主是 BDFL，PR 由其审并合并。数据端两个贡献者对同一事实 mapping 不同 → "谁有原始出处谁赢"；双方都有出处 → 并存成冲突组（这是特性，不是 bug）。

---

## 8. 现有文件导航（给技术新人）

```
history-in-my-hand/
├─ README.md              # 项目是什么、状态表、许可
├─ CONTRIBUTING.md        # 怎么改：DCO / 设计契约 / 治理 / 署名 / 直引约定
├─ COLLABORATE.md         # 本手册（招募与路线图）
├─ LICENSE / LICENSE-code / LICENSE-data   # AGPL-3.0 / CC BY 4.0
├─ data/
│  ├─ scenes.json         # ★ 切片注册表（单一真值）
│  ├─ vocab.json          # ★ 立场/政权受控词表（单一真值）
│  ├─ corridors.json      # 走廊叙事主线
│  ├─ leads.json          # 由缺口聚合的可认领线索（自动生成）
│  ├─ terrain/            # ASTER GDEM 地形网格
│  └─ <县>/               # 每县六件套：sources/places/persons/events/edges/assertions
├─ demo/
│  ├─ portal.html         # 门户（枢纽地图 + 招募入口 + 29 线索）
│  ├─ county.html / county.js   # 通用县页引擎
│  ├─ hub.js              # 枢纽地图（动态并集边界）
│  └─ kaiyuan.css         # 当前样式基线（美工主要战场）
├─ tools/
│  ├─ gates.py            # ★ 一键四闸门（CI 用它）
│  ├─ lint.py / test_assertions.py / leads.py / build.py
│  └─ seed_liaoxi.py      # 加新县的生成器（可复用）
└─ .github/workflows/ci.yml   # PR/push 自动跑 gates
```

> 带 ★ 的是"改动前必须先读懂"的文件；其余可按角色安全触碰。

---

*本手册随项目演进更新。最新版本见仓库根 `COLLABORATE.md`。*
